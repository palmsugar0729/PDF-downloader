# AGENTS.md — AI 开发助手指南

> **面向对象**：后续接手开发的 AI（Claude、Copilot 等）或人类开发者
> **最后更新**：2026-06-05

---

## 项目概述

这是一个 **CLI 桌面工具**，用于从指定网页自动下载 PDF 文件（数学题目的"问题 PDF"和"答案 PDF"），并按规则命名、分文件夹保存。

**目标站点示例**：`https://mikiotaniguchi.com/main/sm/smmain.htm`

**技术栈**：Python 3 + Selenium + Chrome

---

## 文件夹结构及用途

| 文件夹 | 用途 | 规则 |
|--------|------|------|
| `docs/` | 产品文档区 | PRD、需求迭代记录、关键决策记录、开发日志，**所有非代码文档放这里** |
| `assets/design/` | 设计素材 | 效果图、UI 参考（本项目为 CLI，暂时较少使用） |
| `assets/bug/` | 报错截图 | 测试中出现的 bug 截图，方便回溯问题 |
| `assets/reference/` | 参考图、灵感收集 | 目标页面的 DOM 结构截图、命名规则参考等 |
| `notes/` | 学习笔记 | 开发中踩过的坑、学到的技术方案、备忘 |
| `codes/` | **所有代码** | 项目全部源码，详见下方代码区规则 |

---

## 开发规则

### 代码区 (`codes/`)

1. **所有代码文件放在 `codes/` 下**，不在根目录散落 `.py` 文件
2. **代码不引用外部文件夹**（不出现 `../docs`、`../assets` 等路径），保持代码区自包含
3. **入口文件**：`codes/main.py` — CLI 入口
4. **核心模块**：
   - `codes/downloader.py` — 页面解析 + PDF 下载 + 命名逻辑
   - `codes/parser.py` — 标题文本解析（正则提取年份/出处/知识点/识别码）
5. **依赖管理**：在 `codes/` 下放 `requirements.txt`

### 文档区 (`docs/`)

1. **所有规划、需求、决策文档放 `docs/`**
2. 需求迭代记录使用版本号或日期命名，如 `needs.md`、`needs-v2.md`
3. 开发日志使用日期命名：`YYYY-MM-DD_开发日志.md`
4. 关键决策记录（ADR）放在 `docs/decisions/`，文件名格式：`YYYY-MM-DD_{决策主题}.md`

### 其他

1. **`notes/`** — 非正式的随手笔记，格式自由
2. **`assets/`** — 只放图片/截图等媒体文件，不放代码或文档

---

## 已确认的技术决策

| 决策 | 结论 | 日期 |
|------|------|------|
| 程序形态 | CLI 工具（非 GUI），支持交互式输入 | 2026-06-05 |
| 爬取引擎 | Selenium + Chrome | 2026-06-02 |
| 爬取范围 | 单页面，不跟踪子链接 | 2026-06-02 |
| 下载条件 | 只下载同时有「问题 PDF」和「答案 PDF」的题目 | 2026-06-02 |
| 请求策略 | 随机延迟 1~3 秒，遵守 robots.txt，设置合理 UA | 2026-06-02 |
| 标题出处格式 | 任意字符串（不限于以"大"结尾） | 2026-06-05 |
| 文件名唯一性 | 使用 8 位序号的后 6 位作为识别码 | 2026-06-05 |

### 页面 DOM 规律

- 页面使用 `document.write()` 动态生成
- PDF 链接在 `<div align="right">`（或 `align="RIGHT"`）内
- 问题 PDF：`XXXXXXXX.pdf`，标记为 `<p2>問題</p2>`
- 答案 PDF：`XXXXXXXXs.pdf`，标记为 `<p3>解答</p3>`
- 只有 `s.pdf` 没有 `.pdf` → 跳过此题目
- `<a>` 标签可能为空，需同时支持 **标记识别**（`<p2>`/`<p3>`）和 **文件名模式匹配** 两种策略

### 标题解析规则

标题格式：`{8位序号}{知识点}{2位年份}{出处}`

示例：`19112202データの分析19近畿大`

| 部分 | 内容 | 提取方式 |
|------|------|----------|
| 序号 | `19112202` | `^\d{8}` |
| 知识点 | `データの分析` | 序号后、年份前的部分 |
| 年份 | `19` | 出处前的 2 位数字 |
| 出处 | `近畿大` | 剩余任意字符串（不限于以"大"结尾） |

正则：`^(\d{8})(.+?)(\d{2})(.+)$`

### 命名规则

- **8 位序号拆分**：前 2 位 → 年份（补全为 4 位），后 6 位 → **识别码**
- **文件名**：`{补全4位年份}{出处}_{知识点}_{识别码}_问题.pdf` / `_答案.pdf`
- **年份补全**：`≤ 26` → `20xx`，`> 26` → `19xx`
- **保存路径**：`{输出目录}/{出处}/`
- **示例**：`19112202` → 年份 `19` → `2019`，识别码 `112202`，文件名 `2019近畿大_データの分析_112202_问题.pdf`

### CLI 用法

```bash
# 交互模式（直接运行，按提示输入）
cd codes
python main.py

# 参数模式（一次性传入，适合脚本调用）
python main.py "https://target.com/page.htm" -o ./downloads
python main.py "https://target.com/page.htm" -o ./downloads --delay-min 2 --delay-max 5
python main.py "https://target.com/page.htm" --no-headless  # 显示浏览器窗口
```

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `url` | 否 | — | 目标页面地址（不传则交互式输入） |
| `-o, --output` | 否 | `./downloads` | PDF 输出目录 |
| `--delay-min` | 否 | `1` | 请求最小间隔（秒） |
| `--delay-max` | 否 | `3` | 请求最大间隔（秒） |
| `--no-headless` | 否 | — | 显示 Chrome 浏览器窗口（调试用） |
| `-v, --verbose` | 否 | — | 显示详细日志（DEBUG 级别） |

---

## 开发约定

1. **先讨论后编码**：收到新需求时，先确认理解再动工
2. **遇到解析失败的标题**：收集样本，分析规律后更新正则
3. **修改代码后**：更新本文档中对应的技术决策部分
4. **README.md** 面向人类用户，**AGENTS.md** 面向开发者/AI
